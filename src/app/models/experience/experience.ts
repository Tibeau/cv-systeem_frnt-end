export interface Experience {
  id: number;
  description?: string;
  profession: string;
  employer: string;
  company: string;
  city: string;
  country: string;
  startDate: string;
  endDate: string;
  active: boolean;
  candidateId: number;
}
